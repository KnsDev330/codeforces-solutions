#include <bits/stdc++.h>
using namespace std;
using ll = long long;

using vi  = vector<int>;
using vll = vector<ll>;

#define FOR(i, a, b) for(int i = (a); i < (b); ++i)
#define ROF(i, a, b) for(int i = (a); i >= (b); --i)

#ifdef LOCAL
#include "../../debug.h"
#else
#define debug(...)
#endif

map<int, bool> mp;
vector<int> primes;


int main() {
	int n, k, b, s;
	cin >> n >> k >> b >> s;

	return 0;
}

// n k b s
// 5 4 7 38
// 28
// 38-28=10 /5=2
// 38/5
// 0 7 10 10 11
//
//
// 3 6 3 19
//
